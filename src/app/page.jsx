import styles from "./page.module.scss";

const Home = () => {
	return (
		<div className={styles.container}>
			<main>
				<section>
					<p>home</p>
				</section>
			</main>
		</div>
	);
};

export default Home;
